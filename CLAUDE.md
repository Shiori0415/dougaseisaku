# BROOKLYN MUSEUM ／ 向島工房　動画制作

## 資料づくりの恒久ルール

**資料に入れてはいけないもの ── 今後どの資料でも、何においても一切入れない。**

1. 動画をカードで並べた一覧・サマリーのページ
2. 「5人で決めたこと」「共通ルール」「議論の結果」の類のページ
3. 「INSTAGRAMに出す順番」のような配信順の説明ブロック

資料に入れてよいのは **表紙／目次／各本の企画書・構成・絵コンテ** だけ。
議論の結果や判断の根拠は、資料ではなく **チャットで伝える。**

## 絵コンテの形式

- A4横・**1本あたり1ページ**
- 左に企画とテロップ、右に **シーン（縦）× ショット（横）の格子**
- シーンごとに複数の画角の参考写真を並べる
- **参考写真は必ず、いただいた参考動画から抽出する**（ストック画像で代用しない）
- 参考動画がない本は、写真枠を空にして画角の指定を文字で書く

## 答え方のルール

- **どの質問にも、その分野のプロ四人で議論してから答える。**
  （撮影ならディレクター・カメラマン・録音・プロデューサー、
  インタビューなら記者・編集者・メディア担当・広報、のように分野に合わせる）
- 話し口調は端的に。前置きを書かない。
- **資料に入れる日本語は、プロとして直してから出す。**
  文法の崩れ・主語の抜け・二つの問いが一文に混ざっているもの・誘導になっている聞き方は、
  そのまま載せずに直す。直したものは、どこをどう直したかチャットで伝える。

## 言葉のルール

- **テロップはすべて日本語と英語を併記する**
- 数字は漢数字（四割・千百六十グラム）
- 使わない言葉：丁寧・こだわり・職人技・本物・想い

## 画面で読む資料のルール

- **背景は白で固定する。** 見る人の設定が暗い画面でも、資料は白のまま出す
  （`color-scheme: light` を宣言し、暗い画面用の色は書かない）。
- 共通の見た目は `tools/page_style.py` にある。新しいページはここから読み込む。

## 生成方法

### PDF版

```
python3 tools/data_pdf.py          # pdf/kakutei.html を生成
cd pdf && /opt/pw-browsers/chromium --headless --disable-gpu --no-sandbox \
  --no-pdf-header-footer --print-to-pdf="00_動画8本_企画と絵コンテ.pdf" "file://$PWD/kakutei.html"
```

内容は `tools/data_pdf.py` に、版面は `tools/build_pdf.py` にある。
日本語フォントは IPAGothic ／ IPAPGothic。

### Googleスライドで編集できるpptx版

文言・写真の割り当ては `tools/data_pdf.py` が唯一の情報源。`python3 tools/data_pdf.py` を実行すると
`tools/deck_data.json` も同時に書き出され、これを `tools/build_pptx.js`（pptxgenjs）が読んで
`pdf/00_動画8本_企画と絵コンテ.pptx` を生成する。PDF版と内容は同じで、レイアウトのみワイド16:9で作り直している。

```
python3 tools/data_pdf.py          # deck_data.json を書き出す（PDFも同時に更新される）
node tools/build_pptx.js           # pdf/00_動画8本_企画と絵コンテ.pptx を生成（要 npm install pptxgenjs）
```

`.pptx` はGoogleドライブにアップロードして「Googleスライドで開く」でそのまま編集できる。
左カラム（企画・この1本で言うこと・テロップ・追加ブロック）の文章が長いページは、フッターと重ならないよう
`build_pptx.js` が自動でフォントを縮小してレイアウトを収める。

## 決まったこと

### ⑥ OEM工房案内 ── 音の当て方（確定）

語りは全部あとから乗せる。だから作業中に話してもらわない。
**S1はJカット。1問目の声が先に入って、そのあと画が佐藤さんに切り替わる。佐藤さんが映るのはS1から。**
口が動いて映るのは **S8 3枚目** だけ。

三つの原則。

1. **一シーンに、工程の説明・強み・職人のひとことを全部は乗せない。** 二十秒に三つ入れると
   早口になる。**シーンごとに二つまで**（カットが三枚あるS4だけ三つ）。
2. **強みとひとことを交互に置く。** 強みは発注側が判断に使う情報、ひとことは人が見える情報。
   どちらかに偏ると見ていられなくなる。
3. **一つの答えの中で、作業 → 経験 → 強み の順につなげてもらう。**
   説明と感想を別々に録ると、編集でつなぐときに間が空く。
   例：「型を置いて、上から刃を下ろします。裁断です。一度でずれると、革が一枚無駄になります。
   慎重さが要ります。」── ここまで一続きで言ってもらう。

割り当て。

| 秒 | 画 | 乗せるもの |
|---|---|---|
| 0-10 | S1 工房の全体 → **佐藤さん登場（Jカット）** | 創業年 ＋ 強み「一貫して製造」＋ 本人の年数 |
| 10-25 | S2 材料の壁 | 物「壁一面の革。厚みと硬さで棚を分けている」 |
| 25-42 | S3 革の棚・金型 | 工程＋経験「傷と伸びる向きを見る。同じ型でも同じところには置かない」 |
| 42-62 | S4 裁断 | 工程＋経験「一度でずれると革が一枚無駄。慎重さが要ります」＋ 強み「量産も少ない数も」 |
| 62-82 | S5 下仕事・職人の目 | 工程 ＋ ひとこと二つ。**強みは入れない** |
| 82-102 | S6 縫製 | 強み＋経験「角だけは手で。機械が拾えないので、手のほうが確実です」 |
| 102-122 | S7 検品（作業音が消える） | 工程＋経験「照りが揃っていなければ戻します」＋ 思い「一ミリ狂ったら作り直します」 |
| 122-133 | S8 梱包 | 工程 一文だけ。締めのために間を空ける |
| 133 | S8 3枚目 **口が映る** | 強み（回収）「革を選ぶところから箱に入るまで、全部この建物の中で」 |

締めたあとに、尺が余ったとき用の二問を録っておく。**革のおもしろさ** と、
**OEMを考えている会社の方へのひとこと**。カメラを止める前に必ず録る。

- **S5には強みを入れない。** 中盤のいちばん眠いところは、経験則だけで持たせる。
- **S7の静けさは、強みではなく思いに使う。** 作業音が消えるのはここだけ。
- **S8の締めは強みの回収に空けておく。** 冒頭の「一貫して製造」の答え合わせ。

**シーンの中は、① 工程の説明　→　② 自社の強み　→　③ 佐藤さんの経験談 の順。**
説明のあとに売りが来て、そのあとに人の言葉が来る。
強みだけが続くと宣伝、経験談だけが続くと内輪話になる。
**三つ全部を毎回そろえない。** 一つの答えに二つ入るなら、質問は一つでいい。
S8は十一秒しかないので、一問で言い切ってもらう。

**「語りをどこに置くか」のページは資料に入れない。** 質問事項を絵コンテ順に並べてあるので、
上から順に聞けば同じことが済む。表が二つあると、直したときに食い違う。

**佐藤さんの質問は、カテゴリーではなく⑥の絵コンテ順に並べる。**
上から順に聞けば、そのまま画の頭から音が埋まる。あとで音を合わせるときに探さずに済む。
S8までで二分二十秒ぶん。そこから先（人・業界・採用・今後）は本編に使わない。
